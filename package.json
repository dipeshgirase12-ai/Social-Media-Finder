{
  "name": "devtrace",
  "version": "1.0.0",
  "private": true,
  "description": "DevTrace — Public Developer & Profile Discovery Engine. Find. Connect. Understand.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "build:server": "npm --prefix server run build",
    "build:client": "npm --prefix client run build",
    "test": "npm --prefix server run test && npm --prefix client run test -- --run",
    "lint": "npm --prefix server run lint && npm --prefix client run lint",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
